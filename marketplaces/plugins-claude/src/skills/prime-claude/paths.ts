/** Base directory for plugins-claude, relative to project root. */
export const PLUGIN_DIR = '.ai/plugins-claude'

/** Config file path, relative to project root. */
export const CONFIG_PATH = `${PLUGIN_DIR}/prime-claude.config.json`
