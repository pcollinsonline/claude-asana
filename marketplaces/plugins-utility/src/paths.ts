/** Base directory for plugins-utility, relative to project root. */
export const PLUGIN_DIR = '.ai/plugins-utility'

/** Config file path, relative to project root. */
export const CONFIG_PATH = `${PLUGIN_DIR}/config.json`

/** Log directory for hook event files, relative to project root. */
export const LOG_DIR = `${PLUGIN_DIR}/logs`

/** Archive directory for compressed log snapshots, relative to project root. */
export const ARCHIVE_DIR = `${PLUGIN_DIR}/archives`
